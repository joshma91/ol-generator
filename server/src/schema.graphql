scalar DateTime

type Query {
  templates: [Template!]!
}


type Template {
  id: ID!
  description: String!
  name: String!
  createdAt: DateTime!
  account: String
}

type Mutation {
  save(name: String!, description: String!, account: String): Template!
}