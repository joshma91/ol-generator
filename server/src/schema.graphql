scalar DateTime

type Query {
  templates: [Template!]!
}


type Template {
  id: ID!
  description: String!
  name: String!
  createdAt: DateTime!
}

type Mutation {
  save(name: String!, description: String!): Template!
}